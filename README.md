# unicode-font-extractor
This is a lightweight GUI-based Python tool that allows you to extract and compress all used Unicode code points from a given `.ttf` or `.otf` font file. The result is saved as a range-compressed text file
